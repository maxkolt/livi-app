# Опционально: если позже добавите генерацию конфигов через compose env.
# Основная настройка — правка prometheus/prometheus.yml (targets) и
# alertmanager/alertmanager.yml.example → alertmanager.yml (см. ops/README.md).

# Хост:порт бэкенда с точки зрения контейнера Prometheus (GET /metrics)
# Примеры: host.docker.internal:3000 | 172.17.0.1:3000 | backend:3000
# LIVI_METRICS_TARGET=host.docker.internal:3000
