# LiveKit staging. Скопируйте в .env.staging и подставьте те же ключи, что в backend/.env.staging (LIVEKIT_API_KEY / LIVEKIT_API_SECRET).
# Формат: одна строка "key: secret" без пробелов вокруг двоеточия.
LIVEKIT_KEYS=your_api_key:your_api_secret
