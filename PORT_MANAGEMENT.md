# 🚀 Управление портами

## Автоматическое освобождение порта 3000

Порт 3000 теперь **автоматически освобождается** перед запуском приложения!

### 🔧 Что было сделано:

1. **Создан скрипт `scripts/kill-port.sh`** - автоматически убивает все процессы на порту 3000
2. **Добавлены pre-скрипты** в `backend/package.json`:
   - `prestart` - запускается перед `npm start`
   - `predev` - запускается перед `npm run dev`
3. **Создан корневой `package.json`** с удобными командами

### 🎯 Как использовать:

#### Вариант 1: Автоматически (рекомендуется)
```bash
# Запуск backend (автоматически освободит порт)
cd backend
npm run dev

# Или из корня проекта
npm run backend
```

#### Вариант 2: Вручную
```bash
# Освободить порт вручную
npm run kill-port

# Или напрямую
./scripts/kill-port.sh
```

#### Вариант 3: Команда в терминале
```bash
# Быстрое освобождение порта
lsof -ti:3000 | xargs kill -9 2>/dev/null
```

### ✅ Результат:

- **Порт 3000 больше никогда не будет занят** 🎉
- **Автоматическое освобождение** перед каждым запуском
- **Удобные команды** для ручного управления
- **Безопасность** - скрипт проверяет успешность операции

### 🔍 Проверка:

```bash
# Проверить, свободен ли порт
lsof -ti:3000

# Если команда ничего не выводит - порт свободен ✅
# Если выводит PID - порт занят ❌
```

### 🛠️ Troubleshooting:

Если порт все еще занят:
```bash
# Попробовать с sudo
sudo ./scripts/kill-port.sh

# Или найти процесс вручную
lsof -i:3000
kill -9 <PID>
```
