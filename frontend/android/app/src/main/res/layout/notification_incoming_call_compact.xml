<?xml version="1.0" encoding="utf-8"?>
<!-- Компактный вид в шторке: слева только текст (один значок LiVi — в заголовке от системы), справа круглые прозрачные кнопки с рамкой -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="12dp"
    android:paddingVertical="10dp"
    android:minHeight="56dp"
    android:background="@drawable/notification_card_background">

    <!-- Слева: только текст, тап → нативный экран (иконка LiVi одна — в шапке уведомления) -->
    <LinearLayout
        android:id="@+id/notification_content_area"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:minWidth="0dp"
        android:background="?android:attr/selectableItemBackground">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/notification_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:minWidth="0dp"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/notification_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:textColor="#B0B0B0"
                android:layout_marginStart="6dp" />
        </LinearLayout>

        <TextView
            android:id="@+id/notification_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/incoming_call_title"
            android:textSize="13sp"
            android:textColor="#B0B0B0"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Справа: две круглые кнопки — Отклонить (X) и Принять (трубка) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginStart="8dp">

        <ImageView
            android:id="@+id/notification_btn_decline"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="8dp"
            android:background="@drawable/notification_btn_decline_circle"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="@string/incoming_call_decline"
            android:scaleType="centerInside"
            android:tint="#FFFFFF" />

        <ImageView
            android:id="@+id/notification_btn_accept"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="8dp"
            android:background="@drawable/notification_btn_accept_circle"
            android:src="@android:drawable/ic_menu_call"
            android:contentDescription="@string/incoming_call_accept"
            android:scaleType="centerInside"
            android:tint="#FFFFFF"
            android:layout_marginStart="8dp" />
    </LinearLayout>
</LinearLayout>
