diff --git a/bundle/providers/PendingDeepLinkProvider.tsx b/bundle/providers/PendingDeepLinkProvider.tsx
index 1234567..abcdefg 100644
--- a/bundle/providers/PendingDeepLinkProvider.tsx
+++ b/bundle/providers/PendingDeepLinkProvider.tsx
@@ -24,11 +24,31 @@
   const modalStack = useModalStack();
   const [pendingDeepLink, setPendingDeepLink] = React.useState(initialPendingDeepLink);
 
+  const passLiviCallLinkToApp = (url: string, retries = 10): boolean => {
+    if (!url || !/^livi:\/\/(decline-call|answer-call|incoming-call)/.test(url)) return false;
+    const ref = (global as any).__handleCallDeepLinkRef;
+    if (ref?.current) {
+      ref.current(url);
+      return true;
+    }
+    if (retries > 0) {
+      setTimeout(() => passLiviCallLinkToApp(url, retries - 1), 100);
+      return true;
+    }
+    return false;
+  };
+
+  const isLiviCallLink = (u: string) => /^livi:\/\/(decline-call|answer-call|incoming-call)/.test(u);
+
   React.useEffect(() => {
     getPendingDeepLink().then((url) => {
       if (url) {
         setPendingDeepLink(url);
-        modalStack.push(() => <DeepLinkModal pendingDeepLink={url} />);
+        if (isLiviCallLink(url)) {
+          passLiviCallLinkToApp(url);
+        } else if (!passLiviCallLinkToApp(url)) {
+          modalStack.push(() => <DeepLinkModal pendingDeepLink={url} />);
+        }
       }
     });
   }, []);
@@ -37,7 +57,11 @@
     const listener = addDeepLinkListener(({ url }) => {
       if (url) {
         setPendingDeepLink(url);
-        modalStack.push(() => <DeepLinkModal pendingDeepLink={url} />);
+        if (isLiviCallLink(url)) {
+          passLiviCallLinkToApp(url);
+        } else if (!passLiviCallLinkToApp(url)) {
+          modalStack.push(() => <DeepLinkModal pendingDeepLink={url} />);
+        }
       }
     });
