diff --git a/android/src/main/java/io/wazo/callkeep/VoiceConnectionService.java b/android/src/main/java/io/wazo/callkeep/VoiceConnectionService.java
index 0000000..1111111 100644
--- a/android/src/main/java/io/wazo/callkeep/VoiceConnectionService.java
+++ b/android/src/main/java/io/wazo/callkeep/VoiceConnectionService.java
@@ -52,7 +52,10 @@
 import com.facebook.react.bridge.ReadableMap;
 import com.facebook.react.bridge.WritableMap;
 
+import java.text.SimpleDateFormat;
 import java.util.ArrayList;
+import java.util.Date;
+import java.util.Locale;
 import java.util.HashMap;
 import java.util.Iterator;
 import java.util.List;
@@ -290,6 +293,17 @@
         Log.d(TAG, "[VoiceConnectionService] onCreateOutgoingConnection: done");
 
         return outgoingCallConnection;
+    }
+
+    private static PendingIntent getOngoingCallPendingIntent(Context context) {
+        try {
+            Class<?> helperClass = Class.forName("com.kolt12max.livi.LiviOngoingCallHelper");
+            java.lang.reflect.Method m = helperClass.getMethod("getPendingIntent", Context.class);
+            Object result = m.invoke(null, context);
+            return result instanceof PendingIntent ? (PendingIntent) result : null;
+        } catch (Throwable t) {
+            return null;
+        }
     }
 
     private void startForegroundService() {
@@ -314,21 +328,25 @@
         manager.createNotificationChannel(chan);
 
         NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(this, NOTIFICATION_CHANNEL_ID);
+        String title = foregroundSettings.getString("notificationTitle");
+        String timeText = new SimpleDateFormat("HH:mm", Locale.getDefault()).format(new Date());
         notificationBuilder.setOngoing(true)
-            .setContentTitle(foregroundSettings.getString("notificationTitle"))
+            .setContentTitle(title + " " + timeText)
             .setPriority(NotificationManager.IMPORTANCE_MIN)
             .setCategory(Notification.CATEGORY_SERVICE);
 
-        Activity currentActivity = RNCallKeepModule.instance.getCurrentReactActivity();
-        if (currentActivity != null) {
-            Intent notificationIntent = new Intent(this, currentActivity.getClass());
-            notificationIntent.addFlags(Intent.FLAG_ACTIVITY_SINGLE_TOP);
-
-            final int flag =  Build.VERSION.SDK_INT >= Build.VERSION_CODES.M ? PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE : PendingIntent.FLAG_UPDATE_CURRENT;
-
-            PendingIntent pendingIntent = PendingIntent.getActivity(this, NOTIFICATION_ID, notificationIntent, flag);
-
-            notificationBuilder.setContentIntent(pendingIntent);
+        PendingIntent contentIntent = getOngoingCallPendingIntent(this);
+        if (contentIntent == null) {
+            Activity currentActivity = RNCallKeepModule.instance.getCurrentReactActivity();
+            if (currentActivity != null) {
+                Intent notificationIntent = new Intent(this, currentActivity.getClass());
+                notificationIntent.addFlags(Intent.FLAG_ACTIVITY_SINGLE_TOP);
+                final int flag =  Build.VERSION.SDK_INT >= Build.VERSION_CODES.M ? PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE : PendingIntent.FLAG_UPDATE_CURRENT;
+                contentIntent = PendingIntent.getActivity(this, NOTIFICATION_ID, notificationIntent, flag);
+            }
+        }
+        if (contentIntent != null) {
+            notificationBuilder.setContentIntent(contentIntent);
         }
 
         if (foregroundSettings.hasKey("notificationIcon")) {
