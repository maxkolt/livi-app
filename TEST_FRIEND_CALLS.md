# Тест видеозвонков друзьям

## ✅ Что уже реализовано:

### 1. Система звонков друзьям:
- ✅ Кнопка видеозвонка в списке друзей (иконка "video")
- ✅ Функция `startCall()` для инициации звонка
- ✅ Обработчики звонков: `onCallIncoming`, `onCallAccepted`, `onCallDeclined`, `onCallTimeout`
- ✅ Кнопка "Прервать" в VideoChat для завершения звонков
- ✅ События `call:initiate`, `call:accept`, `call:decline`, `call:end`, `call:ended`

### 2. Система статусов "занят":
- ✅ Поле `isBusy` в типе Friend
- ✅ Backend отправляет статус занятости в списке друзей
- ✅ Бэйдж "Занято" с анимацией в InviteButton
- ✅ Обновления статуса через `presence:update`
- ✅ Сброс статуса при завершении звонков

### 3. Интеграция с WebRTC:
- ✅ VideoChat компонент поддерживает звонки друзей
- ✅ Переход в VideoChat с параметрами `directCall: true`
- ✅ Обработка входящих звонков от друзей

## 🧪 Как тестировать:

1. **Запустить приложение** на двух устройствах/эмуляторах
2. **Добавить друг друга в друзья**
3. **Инициировать видеозвонок:**
   - Нажать кнопку видеозвонка (иконка "video") рядом с другом
   - У друга должен появиться бэйдж "Занято" с анимацией
4. **Принять звонок:**
   - У получателя появится модалка входящего звонка
   - При принятии оба перейдут в VideoChat
5. **Завершить звонок:**
   - Нажать кнопку "Прервать" в VideoChat
   - Бэйдж "Занято" должен исчезнуть у обоих

## 🔧 Исправления, которые были сделаны:

1. **Добавлено поле `isBusy` в тип `FriendListItem`**
2. **Backend теперь отправляет статус занятости** в списке друзей
3. **Исправлено маппирование** `isBusy` в HomeScreen
4. **Проверена корректность** обработчиков `presence:update`

## 📱 UI элементы:

- **Кнопка видеозвонка**: иконка "video" рядом с каждым другом
- **Бэйдж "Занято"**: красный бэйдж с анимацией пульсации
- **Модалка входящего звонка**: при получении звонка от друга
- **Кнопка "Прервать"**: в VideoChat для завершения звонков

Система видеозвонков друзьям полностью реализована и готова к тестированию! 🎉
