===========================================
ИСПРАВЛЕНИЕ ФОРМАТА КОНФИГА MONGODB
===========================================

В SSH СЕССИИ ВЫПОЛНИТЕ:

# 1. Посмотреть текущий конфиг
cat /etc/mongod.conf

# 2. Проверить, где находится секция replication
grep -n "replication" /etc/mongod.conf

# 3. Удалить неправильно добавленные строки
# (если replication добавлена как опция, а не секция)

# 4. Правильно добавить секцию replication в YAML формате
# Сначала найдем, где заканчивается секция storage или systemLog

# 5. Исправить конфиг - удалить неправильные строки и добавить правильно
