# Статус проверки сервера

## ✅ Результаты диагностики:

1. **Сервер работает**: `http://89.111.152.241:3000` доступен
2. **API отвечает**: TURN credentials возвращаются корректно
3. **Конфигурация**: URL настроен в `.env` файле

## ⚠️ Проблемы в логах:

1. **Network request failed** на мобильных устройствах
   - iOS: иногда подключается успешно
   - Android: часто не может подключиться

2. **RTCPeerConnection ошибки на Android**
   - "Failed to initialize PeerConnection"
   - Происходит при создании PC для ответа на offer

## 🔍 Возможные причины:

### Для Network request failed:
1. **Неправильный URL для Android** - проверьте `EXPO_PUBLIC_SERVER_URL_ANDROID` в `.env`
2. **Сетевые ограничения** - мобильные устройства могут быть в другой сети
3. **CORS проблемы** - сервер может блокировать запросы с мобильных устройств

### Для RTCPeerConnection:
1. **Нативный модуль WebRTC** - проблема с инициализацией на Android
2. **ICE конфигурация** - может быть проблема с TURN серверами
3. **Одновременное создание PC** - несколько попыток создания одновременно

## 📋 Что проверить:

### На сервере (SSH):
```bash
# Проверить статус PM2
pm2 status

# Проверить логи
pm2 logs livi-backend --lines 50

# Проверить доступность
curl http://localhost:3000/api/turn-credentials
```

### В frontend/.env:
```bash
# Убедитесь, что URL правильные:
EXPO_PUBLIC_SERVER_URL=http://89.111.152.241:3000
EXPO_PUBLIC_SERVER_URL_IOS=http://89.111.152.241:3000
EXPO_PUBLIC_SERVER_URL_ANDROID=http://89.111.152.241:3000
```

### На мобильных устройствах:
- Проверьте, что устройства в той же сети или имеют доступ к интернету
- Проверьте, что файрвол не блокирует подключения

## ✅ Вывод:

**Сервер работает постоянно через PM2** - перезагружать не нужно, если:
1. PM2 настроен с `pm2 startup` и `pm2 save`
2. Процесс не упал (проверьте `pm2 status`)

**Проблемы с подключением** - это скорее проблема конфигурации клиента или сети, а не сервера.

