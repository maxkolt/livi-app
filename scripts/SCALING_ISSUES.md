# Проблемы для масштабирования до 100k+ пользователей

**Статус:** Не готов к масштабированию  
**Текущая готовность:** 1,000–10,000 одновременных пользователей (на одном сервере)  
**Целевая готовность:** 100,000+ пользователей

---

## 🔴 Критические проблемы (блокируют масштабирование)

### 1. Redis не подключен (сейчас fallback на память)

**Лог:**
```
[WARN] [QueueStore] Redis error, falling back to memory
```

**Проблема:**
Без Redis горизонтальное масштабирование невозможно. При 100k+ пользователей:
- Очередь не синхронизируется между серверами
- Каждый сервер работает изолированно
- Невозможен балансинг нагрузки

**Что нужно сделать:**
- [ ] Настроить Redis в продакшене (добавить REDIS_URL в .env)
- [ ] Протестировать подключение Redis
- [ ] Убедиться, что fallback на память работает только в dev режиме
- [ ] Добавить мониторинг состояния Redis соединения

**Файлы для правки:**
- `backend/utils/queueStore.ts` - уже поддерживает Redis, нужно только подключить
- `.env` - добавить `REDIS_URL=redis://localhost:6379` или внешний Redis

---

### 2. Ping timeout warnings (утечки соединений LiveKit)

**Лог:**
```
WARN ping timeout triggered. last pong received at: Tue, 16 Dec 2025 16:11:01 GMT
```

**Проблема:**
Соединения LiveKit не очищаются полностью. При масштабировании:
- Накопление "зомби" соединений
- Утечки памяти на LiveKit сервере
- Лишняя нагрузка на ping/pong механизм

**Что нужно сделать:**
- [ ] Улучшить cleanup соединений LiveKit перед переподключением
- [ ] Убедиться, что `room.disconnect()` полностью очищает все handlers
- [ ] Добавить проверку состояния комнаты перед операциями
- [ ] Увеличить таймауты или улучшить обработку ping/pong

**Файлы для правки:**
- `frontend/src/webrtc/sessions/RandomChatSession.ts` - метод `disconnectRoom()`
- `frontend/src/webrtc/sessions/VideoCallSession.ts` - метод `disconnectRoom()`

---

### 3. "could not find local track subscription"

**Лог:**
```
WARN could not find local track subscription for subscribed event
```

**Проблема:**
Синхронизация треков нарушена. При масштабировании:
- Увеличение ложных предупреждений
- Возможные проблемы с отображением видео/аудио
- Лишняя нагрузка на обработку событий

**Что нужно сделать:**
- [ ] Проверить логику подписки на локальные треки
- [ ] Убедиться, что треки правильно регистрируются перед подпиской
- [ ] Добавить проверку существования трека перед обработкой события
- [ ] Игнорировать события для локальных треков (если это не нужно)

**Файлы для правки:**
- `frontend/src/webrtc/sessions/RandomChatSession.ts` - обработка событий треков
- `frontend/src/webrtc/sessions/VideoCallSession.ts` - обработка событий треков

---

### 4. "could not createOffer with closed peer connection"

**Лог:**
```
WARN could not createOffer with closed peer connection
```

**Проблема:**
Попытки создать offer после закрытия соединения. При масштабировании:
- Множественные попытки на закрытых соединениях
- Лишняя нагрузка на WebRTC стек
- Race conditions при быстрых переподключениях

**Что нужно сделать:**
- [ ] Добавить проверку состояния соединения перед `createOffer`
- [ ] Улучшить синхронизацию между disconnect и операциями WebRTC
- [ ] Добавить флаги для отслеживания состояния соединения
- [ ] Отменять pending операции при закрытии соединения

**Файлы для правки:**
- `frontend/src/webrtc/sessions/RandomChatSession.ts` - методы работы с WebRTC
- `frontend/src/webrtc/sessions/VideoCallSession.ts` - методы работы с WebRTC

---

### 5. "detected connection state mismatch" (новая проблема)

**Лог:**
```
WARN detected connection state mismatch
```

**Проблема:**
Несоответствие состояния соединения. При масштабировании:
- Некорректное состояние соединений
- Возможные сбои при переподключениях
- Проблемы с синхронизацией между клиентом и сервером

**Что нужно сделать:**
- [ ] Улучшить синхронизацию состояния соединения
- [ ] Добавить проверки состояния перед операциями
- [ ] Улучшить обработку переподключений
- [ ] Добавить логирование для диагностики mismatch

**Файлы для правки:**
- `frontend/src/webrtc/sessions/RandomChatSession.ts` - управление состоянием комнаты
- `frontend/src/webrtc/sessions/VideoCallSession.ts` - управление состоянием комнаты

---

### 6. "websocket closed" (новая проблема)

**Лог:**
```
WARN websocket closed {"code": 1001, "reason": "Stream end encountered"}
```

**Проблема:**
Неожиданные закрытия WebSocket соединений. При масштабировании:
- Потеря соединений при активных сессиях
- Плохой UX для пользователей
- Лишние переподключения

**Что нужно сделать:**
- [ ] Улучшить обработку закрытия WebSocket соединений
- [ ] Добавить автоматическое переподключение с экспоненциальной задержкой
- [ ] Улучшить обработку ошибок соединения
- [ ] Добавить уведомления пользователя о проблемах соединения

**Файлы для правки:**
- `frontend/src/webrtc/sessions/RandomChatSession.ts` - обработка событий комнаты
- `frontend/src/webrtc/sessions/VideoCallSession.ts` - обработка событий комнаты
- `frontend/sockets/socket.ts` - обработка переподключений

---

## 🟡 Средние проблемы (могут замедлить, но не блокируют)

### 7. Множественные пересоздания Peer Connections

**Лог:**
```
rn-webrtc:pc:DEBUG 0 ctor +0ms
rn-webrtc:pc:DEBUG 1 ctor +4ms
...
```

**Проблема:**
В логах много `rn-webrtc:pc:DEBUG` — частые пересоздания. При масштабировании:
- Высокое потребление ресурсов
- Увеличение нагрузки на TURN/STUN серверы
- Медленнее установка соединений

**Что нужно сделать:**
- [ ] Оптимизировать переиспользование Peer Connections
- [ ] Уменьшить количество пересозданий соединений
- [ ] Добавить пулинг соединений (если возможно)
- [ ] Улучшить логику переподключений

**Файлы для правки:**
- `frontend/src/webrtc/sessions/RandomChatSession.ts` - управление соединениями
- `frontend/src/webrtc/sessions/VideoCallSession.ts` - управление соединениями

---

### 8. Отсутствие метрик/мониторинга

**Проблема:**
Нет метрик размера очереди, времени ожидания, успешности матчей. При масштабировании:
- Сложно диагностировать проблемы
- Невозможно отследить узкие места
- Нет данных для оптимизации

**Что нужно сделать:**
- [ ] Добавить метрики размера очереди ожидания
- [ ] Добавить метрики времени ожидания в очереди
- [ ] Добавить метрики успешности матчей
- [ ] Добавить метрики количества активных пар
- [ ] Добавить метрики использования Redis vs Memory
- [ ] Интегрировать с системой мониторинга (Prometheus, Grafana, etc.)

**Файлы для правки:**
- `backend/utils/queueStore.ts` - добавить экспорт метрик
- `backend/sockets/match.ts` - добавить логирование метрик
- `backend/index.ts` - добавить endpoint для метрик

---

## 📋 Чеклист перед масштабированием

### Обязательные исправления:
- [ ] **Redis подключен и протестирован в продакшене**
- [ ] **Ping timeout warnings устранены**
- [ ] **"could not find local track subscription" исправлено**
- [ ] **"could not createOffer" исправлено**
- [ ] **"connection state mismatch" исправлено**
- [ ] **"websocket closed" обработано корректно**

### Желательные улучшения:
- [ ] Оптимизированы пересоздания Peer Connections
- [ ] Добавлены метрики и мониторинг
- [ ] Настроен алертинг для критических метрик
- [ ] Проведено нагрузочное тестирование на 10k+ пользователей

---

## 📊 Текущее состояние

**Готов для:** 1,000–10,000 одновременных пользователей (на одном сервере)  
**Не готов для:** 100,000+ пользователей (требуются исправления выше)

**Вывод:** Архитектура улучшена (Redis, rate limiting, cleanup), но остаются критические проблемы с WebRTC и LiveKit, которые блокируют масштабирование до сотен тысяч пользователей.

---

## 🔧 Приоритет исправлений

1. **Высокий приоритет:**
   - Redis подключение
   - Ping timeout warnings
   - "could not createOffer"
   - "connection state mismatch"

2. **Средний приоритет:**
   - "could not find local track subscription"
   - "websocket closed"
   - Метрики и мониторинг

3. **Низкий приоритет:**
   - Оптимизация пересозданий Peer Connections

---

## 📝 Заметки

- Все исправления должны быть протестированы на staging окружении
- После каждого исправления нужно проверить логи на отсутствие новых warnings
- Рекомендуется проводить нагрузочное тестирование после каждого крупного исправления
- Документировать все изменения для будущей поддержки

---

**Дата создания:** 2025-12-16  
**Последнее обновление:** 2025-12-16


