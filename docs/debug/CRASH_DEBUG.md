# 🔍 Диагностика краша приложения

## Проблема: Приложение сразу закрывается после открытия

### Быстрая диагностика

**1. Получить логи Android:**
```bash
./debug-crash.sh
```

Или вручную:
```bash
# Очистить логи
adb logcat -c

# Мониторить логи
adb logcat | grep -iE "livi|error|exception|fatal|crash"
```

**2. Открыть приложение на устройстве**

**3. Скопировать ошибки из логов**

---

## Возможные причины краша

### 1. ❌ Отсутствуют переменные окружения CometChat

**Симптомы:**
- Приложение закрывается сразу после открытия
- В логах: `CometChat initialization failed` или `undefined`

**Решение:**
- Проверьте что переменные настроены в EAS Secrets:
  ```bash
  eas secret:list
  ```

- Если переменных нет, добавьте их:
  ```bash
  eas secret:create --name EXPO_PUBLIC_COMETCHAT_APP_ID --value YOUR_APP_ID --type string
  eas secret:create --name EXPO_PUBLIC_COMETCHAT_REGION --value YOUR_REGION --type string
  eas secret:create --name EXPO_PUBLIC_COMETCHAT_AUTH_KEY --value YOUR_AUTH_KEY --type string
  ```

- Пересоберите AAB:
  ```bash
  cd frontend/android
  ./gradlew bundleRelease
  ```

### 2. ❌ Проблемы с подключением к серверу

**Симптомы:**
- Приложение закрывается при попытке подключения
- В логах: `Network request failed` или `Connection refused`

**Решение:**
- Проверьте что backend работает:
  ```bash
  curl https://api.liviapp.com/health
  ```

- Проверьте переменные окружения:
  ```bash
  eas secret:list | grep EXPO_PUBLIC_SERVER_URL
  ```

### 3. ❌ Ошибки в нативных модулях

**Симптомы:**
- В логах: `UnsatisfiedLinkError` или `ClassNotFoundException`
- Ошибки связанные с `react-native-reanimated`, `livekit`, и т.д.

**Решение:**
- Пересоберите AAB с чистым кешем:
  ```bash
  cd frontend/android
  ./gradlew clean
  ./gradlew bundleRelease
  ```

### 4. ❌ Проблемы с разрешениями

**Симптомы:**
- В логах: `Permission denied`
- Приложение запрашивает разрешения и падает

**Решение:**
- Проверьте `AndroidManifest.xml` - все разрешения должны быть объявлены

---

## Пошаговая диагностика

### Шаг 1: Получить логи
```bash
./debug-crash.sh
```

### Шаг 2: Найти ошибку
Ищите в логах:
- `FATAL EXCEPTION`
- `AndroidRuntime`
- `com.kolt12max.livi`
- `Error:` или `Exception:`

### Шаг 3: Проверить переменные окружения
```bash
# Проверить EAS Secrets
eas secret:list

# Проверить локальный .env (для разработки)
cat frontend/.env | grep EXPO_PUBLIC
```

### Шаг 4: Проверить backend
```bash
curl https://api.liviapp.com/health
# Должно вернуть: {"ok":true,"mongo":1}
```

### Шаг 5: Пересобрать с исправлениями
```bash
cd frontend/android
./gradlew clean bundleRelease
```

---

## Частые ошибки и решения

### Ошибка: `CometChat initialization failed`
**Причина:** Отсутствуют переменные окружения  
**Решение:** Настроить EAS Secrets (см. выше)

### Ошибка: `Network request failed`
**Причина:** Backend недоступен или неправильный URL  
**Решение:** Проверить backend и переменные `EXPO_PUBLIC_SERVER_URL`

### Ошибка: `UnsatisfiedLinkError`
**Причина:** Проблемы с нативными модулями  
**Решение:** Пересобрать с `./gradlew clean`

### Ошибка: `Permission denied`
**Причина:** Не объявлены разрешения в манифесте  
**Решение:** Проверить `AndroidManifest.xml`

---

## Полезные команды

```bash
# Получить логи приложения
adb logcat | grep -i livi

# Очистить кеш приложения
adb shell pm clear com.kolt12max.livi

# Переустановить приложение
adb install -r frontend/android/app/build/outputs/apk/release/app-release.apk

# Проверить переменные окружения в build
cd frontend/android
./gradlew :app:assembleRelease --info | grep EXPO_PUBLIC
```

---

## Если ничего не помогает

1. Получите полные логи:
   ```bash
   adb logcat > crash_logs.txt
   ```

2. Откройте приложение

3. После краша остановите логи (Ctrl+C)

4. Отправьте `crash_logs.txt` для анализа
